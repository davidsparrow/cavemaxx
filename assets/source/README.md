Original Canva exports of the bone lettering, kept for re-editing. The files Claude actually
uses are `../title-todo.svg` and `../title-question.svg`: the same artwork cropped to the
letters, with the fill set to `currentColor` and coordinates rounded so they are about half
the size. This folder is left out of the release package.
