---
description: CaveMaxx off. Text and Markdown unless you ask for an interface.
argument-hint: "[request]"
---
The user ran `/cave-off`. Switch CaveMaxx to `/cave-off` mode and behave as the CaveMaxx skill
describes for that mode. If the CaveMaxx skill is not already in your context, load it first:
it is the `cavemaxx` skill in this plugin (`cavemaxx:cavemaxx`), whose SKILL.md sits at the
plugin root next to this commands folder.

Acknowledge the switch in one line, then handle whatever follows, if anything:

$ARGUMENTS
